

<h3><%= @user.username %></h3>
<%= link_to "Edit Account",user_controller_edit_path %>
  <hr>
<h4>Entries</h4>


<div class="user-show-posts">
  <h4>Most Recent</h4>
  <hr>
  <% @posts.each do |post| %>
  <%= will_paginate @posts %>
    <p><%= post.title %></p>
    <p><%= truncate(post.content, length: 100) %></p>

    <p>Created <%= time_ago_in_words (post.created_at) %></p>

    <hr>

</div>
<% end %>
